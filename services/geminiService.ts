// AI features have been disabled.
// This file is kept to prevent import errors if it's referenced elsewhere, 
// but the implementation is now a stub.

export const enhanceMarkdown = async (content: string, instruction: string): Promise<string> => {
  throw new Error("AI features are disabled in this version.");
};